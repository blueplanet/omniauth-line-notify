require 'spec_helper'

describe Omniauth::Line::Notify do
  it 'has a version number' do
    expect(Omniauth::Line::Notify::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
